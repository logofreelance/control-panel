/**
 * feature-settings — TypeScript Interfaces
 * 
 * 🤖 AI: Single source of truth for settings data shapes.
 */

export interface SiteSettings {
    siteName: string;
    siteTitle: string;
    primaryColor: string;
    faviconUrl: string;
    metaDescription: string;
    themePreset: string;
}
