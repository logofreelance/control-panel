import { Loader2, ChevronLeft, User, KeyRound, LogOut } from 'lucide-react';

export const Icons = {
    loading: Loader2,
    chevronLeft: ChevronLeft,
    user: User,
    key: KeyRound,
    logout: LogOut,
} as const;
