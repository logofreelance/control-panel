import {
    ChevronLeft, User, Plus, X, Network, ArrowRight,
    Activity, Pencil, Trash2, Zap, Database, Save, Loader2, Search,
} from 'lucide-react';

export const Icons = {
    chevronLeft: ChevronLeft,
    user: User,
    plus: Plus,
    close: X,
    network: Network,
    arrowRight: ArrowRight,
    activity: Activity,
    pencil: Pencil,
    trash: Trash2,
    zap: Zap,
    database: Database,
    save: Save,
    loading: Loader2,
    search: Search,
} as const;
